<% form_for @page do |f| %>
  <p>
    <%= f.label :keywords %>
    <%= f.text_field :keywords %>
  </p>

  <p>
    <%= f.label :category_id %> 
    <%= f.collection_select :category_id, Category.all, :horses, :players, :include_blank => true %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
