<%= form_for(@page, :html => {:multipart => true}) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="title">
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %><br />
  </div>

<% end %>
